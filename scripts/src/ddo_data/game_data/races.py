"""Parse DDO race data from extracted game data."""

from pathlib import Path


def parse_races(data_dir: Path) -> list[dict]:
    """Parse race definitions and return as list of dicts."""
    return []
