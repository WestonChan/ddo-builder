"""Parse DDO feat definitions from extracted game data."""

from pathlib import Path


def parse_feats(data_dir: Path) -> list[dict]:
    """Parse feat definitions and return as list of dicts."""
    return []
