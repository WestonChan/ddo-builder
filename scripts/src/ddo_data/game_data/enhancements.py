"""Parse DDO enhancement tree data from extracted game data."""

from pathlib import Path


def parse_enhancements(data_dir: Path) -> list[dict]:
    """Parse enhancement tree data and return as list of dicts."""
    return []
