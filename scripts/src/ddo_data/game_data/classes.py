"""Parse DDO class data from extracted game data."""

from pathlib import Path


def parse_classes(data_dir: Path) -> list[dict]:
    """Parse class definitions and return as list of dicts."""
    return []
